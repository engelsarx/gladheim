var Utilities = (function (window, document, undefined) {

  "use strict";

  var ej = function (msg) {
    msg = msg | 'message';
    return msg;
  };

  return {
    ej: ej
  };

}(window, document));
